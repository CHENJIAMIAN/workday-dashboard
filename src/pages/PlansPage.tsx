import { CountdownList } from '../components/CountdownList';
import { ZoneList } from '../components/ZoneList';
import type { CountdownView, TimeZoneView } from '../components/types';

export interface PlansPageProps { countdowns: CountdownView[]; zones: TimeZoneView[]; onAddCountdown: () => void; onEditCountdown: (item: CountdownView) => void; onDeleteCountdown: (id: string) => void; onAddZone: () => void; onEditZone: (zone: TimeZoneView) => void; onDeleteZone: (id: string) => void; }
export function PlansPage(props: PlansPageProps) { return <main className="page"><div className="page-heading"><div><p className="eyebrow">未来安排</p><h1>计划</h1><p>把需要关注的日期与时间分区放在这里。</p></div><button className="primary-button" type="button" onClick={props.onAddCountdown}>＋ 新建倒计时</button></div><section className="page-section"><h2>长期倒计时</h2><CountdownList items={props.countdowns} onEdit={props.onEditCountdown} onDelete={props.onDeleteCountdown} /></section><section className="page-section"><div className="section-heading"><h2>时间分区</h2><button className="text-button" type="button" onClick={props.onAddZone}>新建分区</button></div><ZoneList zones={props.zones} onEdit={props.onEditZone} onDelete={props.onDeleteZone} /></section></main>; }
