export interface SettingsValue { unitMinutes: number; birthDate: string; endTime?: string; }
export interface SettingsDrawerProps { open: boolean; value: SettingsValue; onChange: (value: SettingsValue) => void; onClose: () => void; onExport: () => void; onOpenImport: () => void; onOpenDrive: () => void; }
export function SettingsDrawer({ open, value, onChange, onClose, onExport, onOpenImport, onOpenDrive }: SettingsDrawerProps) { return <>{open && <button className="drawer-backdrop" type="button" aria-label="关闭设置" onClick={onClose} />}<aside className={open ? 'settings-drawer is-open' : 'settings-drawer'} aria-hidden={!open}><div className="drawer-header"><div><p className="eyebrow">偏好与数据</p><h2>设置</h2></div><button className="icon-button" type="button" onClick={onClose}>×</button></div><div className="settings-group"><label>每个时间块（分钟）<input type="number" min="1" max="120" value={value.unitMinutes} onChange={(e) => onChange({ ...value, unitMinutes: Number(e.target.value) })} /></label><label>出生日期<input type="date" value={value.birthDate} onChange={(e) => onChange({ ...value, birthDate: e.target.value })} /></label>{value.endTime !== undefined && <label>下班时间<input type="time" value={value.endTime} onChange={(e) => onChange({ ...value, endTime: e.target.value })} /></label>}</div><div className="settings-group"><h3>数据</h3><button className="setting-action" type="button" onClick={onExport}>导出 JSON 备份 <span>›</span></button><button className="setting-action" type="button" onClick={onOpenImport}>导入备份 <span>›</span></button><button className="setting-action" type="button" onClick={onOpenDrive}>Google Drive <span>›</span></button></div></aside></>; }
