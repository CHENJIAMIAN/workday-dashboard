export interface ImportDialogProps { open: boolean; error?: string; onClose: () => void; onImport: (file: File) => void; }
export function ImportDialog({ open, error, onClose, onImport }: ImportDialogProps) { if (!open) return null; return <div className="modal-layer"><div className="dialog"><div className="dialog-header"><h2>导入 JSON 备份</h2><button className="icon-button" type="button" onClick={onClose}>×</button></div><p>支持现有 WorkDay 备份。导入前会校验内容。</p><label className="file-drop">选择 JSON 文件<input type="file" accept="application/json,.json" onChange={(e) => { const file = e.target.files?.[0]; if (file) onImport(file); }} /></label>{error && <p className="error-message" role="alert">{error}</p>}</div></div>; }
